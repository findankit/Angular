import { ProperHeadingPipe } from './proper-heading.pipe';

describe('ProperHeadingPipe', () => {
  it('create an instance', () => {
    const pipe = new ProperHeadingPipe();
    expect(pipe).toBeTruthy();
  });
});
